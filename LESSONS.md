# Lessons — Skill-Level Learnings from Real Runs

Append-only. Each makeit run ends with a retro step (see `phases/08-polish.md`); lessons that
generalize beyond one project get upstreamed here. A lesson that exposes a rule loophole also
becomes a pressure scenario in `tests/pressure-scenarios.md`.

Format: `- [YYYY-MM-DD, project-type/profile] what surprised us -> what changed (or should)`

## Lessons

(none yet — awaiting the first real run)
